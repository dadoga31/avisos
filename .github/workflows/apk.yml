name: APK de Android

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - claude/app-gestion-avisos-408zhn
    paths:
      - 'android/**'
      - 'assets/**'
      - 'index.html'
      - 'manifest.webmanifest'
      - '.github/workflows/apk.yml'

permissions:
  contents: write

jobs:
  compilar:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Preparar Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      # Se compila en dos pasos: primero solo el Java, que es donde suelen
      # estar los errores y falla en segundos.
      - name: Compilar el código
        working-directory: android
        shell: bash
        run: |
          set -o pipefail
          ./gradlew compileReleaseJavaWithJavac --no-daemon --console=plain \
            -Dorg.gradle.internal.http.socketTimeout=60000 2>&1 | tee ../compilacion.log

      - name: Empaquetar el APK
        working-directory: android
        shell: bash
        run: |
          set -o pipefail
          ./gradlew assembleRelease --no-daemon --console=plain 2>&1 | tee -a ../compilacion.log

      # Sin esto, el rastro de Gradle entierra el error de verdad.
      - name: Qué ha fallado
        if: failure()
        shell: bash
        run: |
          echo "===== ERRORES ====="
          grep -nE "error:|^e: |What went wrong|Caused by:|Could not (find|resolve)" compilacion.log | head -60 || true

      - name: Renombrar
        run: cp android/app/build/outputs/apk/release/app-release.apk avisos.apk

      - name: Guardar como artefacto
        uses: actions/upload-artifact@v4
        with:
          name: avisos-apk
          path: avisos.apk
          if-no-files-found: error

      - name: Publicar en la release «apk»
        if: github.event_name == 'workflow_dispatch' || github.ref_name == 'main'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release delete apk --yes --cleanup-tag 2>/dev/null || true
          gh release create apk avisos.apk \
            --title "Avisos · última APK" \
            --notes "APK generada automáticamente desde ${{ github.sha }}. Descarga avisos.apk e instálala en el móvil." \
            --target "${{ github.sha }}"
